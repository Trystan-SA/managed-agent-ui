# 1.0.0 — Architecture & Overview

## 1.1.0 System Purpose

A self-hosted, multi-user dashboard for orchestrating Anthropic Managed Agents. Provides CRUD management for agents, environments, and sessions; a real-time chat interface with SSE streaming; an in-process scheduler for recurring agent runs; and per-user MCP credential management via Anthropic vaults. The first registered account becomes admin; additional users are added via invite.

## 1.2.0 Architecture Pattern

**Monolithic SvelteKit application** — a single process serves both the SSR/CSR frontend and REST API routes. No microservices, no BFF pattern.

```
┌─────────────────────────────────────────────┐
│  Docker Compose                             │
│  ┌──────────────────────┐  ┌─────────────┐ │
│  │  SvelteKit App       │  │  Postgres   │ │
│  │  (Node adapter)      │──│  5432       │ │
│  │  Port 3000           │  └─────────────┘ │
│  │                      │                   │
│  │  Server routes ──────│──► Anthropic API  │
│  │  Pages/UI            │    (proxied)      │
│  │                      │──► SMTP (optional)│
│  └──────────────────────┘                   │
└─────────────────────────────────────────────┘
```

### 1.2.1 Rationale

- Multi-user with role-based access (admin/member) — single deployment serves a team.
- SvelteKit's server routes provide type-safe API endpoints colocated with their consumers.
- Docker Compose bundles both services for one-command deployment.
- Optional SMTP integration for email-based invites; falls back to manual invite URLs.

## 1.3.0 Tech Stack

| Layer | Technology | Version |
|-------|-----------|---------|
| Frontend | Svelte 5, SvelteKit | 2.57 |
| Styling | SCSS (design tokens) | — |
| Backend | SvelteKit server routes | Node adapter |
| Database | PostgreSQL | 16 |
| ORM | Drizzle | 0.45 |
| API Client | Anthropic SDK | 0.86 |
| Auth | bcrypt, AES-256-GCM | — |
| Runtime | Node.js | 22 |
| Deployment | Docker Compose | — |

## 1.4.0 Data Ownership

**Anthropic API is the source of truth** for agents, environments, sessions, and MCP credentials (stored in per-user vaults). The local PostgreSQL database stores:

- User authentication (email, hashed password, role)
- Database-backed auth sessions (token, expiry)
- Encrypted global API key (shared across all users, admin-managed)
- User invites (token, expiry, optional temporary password)
- Bookmarks (nicknames, pins) — local metadata over Anthropic resources, user-scoped
- User preferences (theme, defaults)
- SMTP settings (encrypted, for invite emails)
- **Vault pointer** (`users.vault_id`) — per-user Anthropic vault ID; see [10.0.0](./10.0.0-connections.md)
- **OAuth provider configs** (encrypted client_id + client_secret per MCP service) and short-lived OAuth state tokens
- **Scheduled tasks** + their execution history and edit history; see [11.0.0](./11.0.0-scheduled-tasks.md)

Local DB never caches or duplicates Anthropic API state — MCP credentials live entirely in the user's Anthropic vault.

## 1.4.1 Domain Boundaries

Each domain owns its API routes and pages under `src/routes/`. Detailed specs are in the indexed chapters:

| # | Domain | API prefix | Pages |
|---|--------|-----------|-------|
| [2](./2.0.0-auth.md) | Auth | `/api/auth/*` | `/login`, `/register`, `/invite/[token]`, `/settings/reset-password` |
| [3](./3.0.0-agents.md) | Agents | `/api/agents/*` | `/agents/*` |
| [4](./4.0.0-environments.md) | Environments | `/api/environments/*` | `/environments/*` |
| [5](./5.0.0-sessions.md) | Sessions | `/api/sessions/*` | (listing folded into `/dashboard`) |
| [6](./6.0.0-chat.md) | Chat | `/api/sessions/[id]/{events,stream}` | `/dashboard`, `/chat/[id]` |
| [7](./7.0.0-dashboard.md) | Dashboard | — | `/dashboard` |
| [8](./8.0.0-settings.md) | Settings + admin | `/api/settings/*`, `/api/admin/*` | `/settings` |
| [9](./9.0.0-design-system.md) | Design system | — | — |
| [10](./10.0.0-connections.md) | Connections (MCP + OAuth) | `/api/{connections,vaults,admin/oauth-providers}` | `/connections` |
| [11](./11.0.0-scheduled-tasks.md) | Scheduled tasks | `/api/scheduled-tasks/*` | Schedules embedded in `/agents/[id]/edit` |

## 1.5.0 Deployment

### 1.5.1 Production

- Multi-stage Docker build (builder → production) via `docker/Dockerfile`
- `docker/docker-entrypoint.sh` runs Drizzle migrations then starts the app
- Health checks for Postgres readiness before app starts
- Persistent `pgdata` volume for database storage
- `docker compose --env-file .env -f docker/docker-compose.yml up` — runs on port 3000

### 1.5.2 Development

- Single-stage `docker/Dockerfile.dev` — installs deps, runs Vite dev server
- `docker/docker-compose.dev.yml` — volume-mounts `src/`, `static/`, config files, and migrations
- Vite HMR with filesystem polling (1s interval) for Docker volume compatibility
- Postgres exposed on port 5432 for direct access
- `docker compose --env-file .env -f docker/docker-compose.dev.yml up --build` — runs on port 5173
- Separate `pgdata_dev` volume to isolate dev data from production

## 1.6.0 CI/CD

GitHub Actions pipeline (`.github/workflows/ci.yml`) runs on push and PR to `main`:

1. **Lint** — `npm run lint` (ESLint with TypeScript + Svelte plugins)
2. **Type check** — `npm run check` (svelte-check + TypeScript)
3. **Test** — `npm run test` (Vitest)

Run locally with `npm run ci`.

## 1.7.0 Environment Variables

| Variable | Required | Description |
|----------|----------|-------------|
| `POSTGRES_HOST` | No | PostgreSQL host (default: `postgres`) |
| `POSTGRES_PASSWORD` | Yes | PostgreSQL password |
| `POSTGRES_USER` | No | PostgreSQL user (default: `managed_agents`) |
| `POSTGRES_DB` | No | Database name (default: `managed_agents`) |
| `ENCRYPTION_KEY` | Yes | 64-char hex for AES-256-GCM |
| `SESSION_SECRET` | Yes | Cookie signing secret |
| `SETUP_PASSWORD` | Yes* | One-time setup password (*removable after setup) |
| `PORT` | No | App port (default: `3000`) |
